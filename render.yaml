services:
  - type: web
    name: meta-navigator-bot
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: AI_API_KEY
        sync: false
      - key: PORT
        value: 8080
    healthCheckPath: /health

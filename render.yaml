services:
  - type: web
    name: meta-navigator-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python render_start.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: AI_API_KEY
        sync: false
      - key: PORT
        value: 8080

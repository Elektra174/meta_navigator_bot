import os
import asyncio
import traceback
import logging
import re
import signal
import sys
import json
from datetime import datetime
from aiogram import Bot, Dispatcher, types, F
from aiogram.filters import Command
from aiogram.fsm.context import FSMContext
from aiogram.fsm.state import State, StatesGroup
from aiogram.fsm.storage.memory import MemoryStorage
from aiogram.utils.keyboard import InlineKeyboardBuilder, ReplyKeyboardBuilder
from aiohttp import web, ClientSession

# =================================================================================================
# 1. СИСТЕМНЫЕ НАСТРОЙКИ И БЕЗОПАСНОСТЬ
# =================================================================================================

# Настройка сигналов для корректной работы на Linux/Render (предотвращает зомби-процессы)
if sys.platform != 'win32':
    signal.signal(signal.SIGALRM, signal.SIG_IGN)
    signal.signal(signal.SIGCHLD, signal.SIG_IGN)

# Попытка импорта Cerebras SDK с обработкой отсутствия библиотеки
try:
    from cerebras.cloud.sdk import AsyncCerebras
    CEREBRAS_AVAILABLE = True
except ImportError:
    CEREBRAS_AVAILABLE = False

# Загрузка переменных окружения
TOKEN = os.getenv("BOT_TOKEN")
AI_KEY = os.getenv("AI_API_KEY")

# Основные ID администратора и канала
CHANNEL_ID = "@metaformula_life"
ADMIN_ID = 7830322013

# =================================================================================================
# 2. РЕСУРСЫ И ССЫЛКИ (КОНТЕНТ v4.8)
# =================================================================================================

# Ссылки на медиа
LOGO_URL = "https://raw.githubusercontent.com/Elektra174/meta_navigator_bot/main/logo.png"
LOGO_NAVIGATOR_URL = "https://raw.githubusercontent.com/Elektra174/meta_navigator_bot/main/logo11.png"

# Файлы и продукты
PROTOCOL_URL = "https://raw.githubusercontent.com/Elektra174/meta_navigator_bot/main/Autopilot_System_Protocol.pdf"
# Временная заглушка на YouTube, как ты просил
PRACTICUM_URL = "https://www.youtube.com/@МетаформулаЖизни" 
CHANNEL_LINK = "https://t.me/metaformula_life"
SUPPORT_LINK = "https://t.me/lazalex81"

# Настройка логирования (Подробный вывод для отладки)
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[logging.StreamHandler(sys.stdout)]
)
logger = logging.getLogger(__name__)

# Инициализация бота и диспетчера состояний
bot = Bot(token=TOKEN)
dp = Dispatcher(storage=MemoryStorage())

# Инициализация AI клиента (с защитой от сбоев)
ai_client = None
if AI_KEY and CEREBRAS_AVAILABLE:
    try:
        ai_client = AsyncCerebras(api_key=AI_KEY)
        logger.info("✅ Cerebras AI Engine: ONLINE (Identity Lab v4.8)")
    except Exception as e:
        logger.error(f"❌ AI Engine Init Error: {e}")
        ai_client = None
else:
    logger.warning("⚠️ Cerebras AI недоступен. Активирован режим локального анализатора (Regex Fallback).")

# Глобальные переменные для мониторинга и веб-отчетов
start_time = datetime.now()
diagnostic_data = {}  # Хранилище отчетов: {user_id: {data}}

# Определение машины состояний (FSM)
class AuditState(StatesGroup):
    answering = State()

# =================================================================================================
# 3. МЕТОДОЛОГИЯ: ВОПРОСЫ (IDENTITY LAB v4.8 - СУБЪЕКТНЫЙ ПОДХОД)
# =================================================================================================

QUESTIONS = [
    # 1. Локация
    "📍 **Точка 1: Локация.**\nВ какой сфере жизни или в каком деле ты сейчас чувствуешь пробуксовку? Опиши ситуацию, где твои усилия не дают того результата, на который ты рассчитываешь.",
    
    # 2. Мета-Маяк
    "📍 **Точка 2: Мета-Маяк.**\nПредставь, что задача решена на 100%. Опиши свое состояние в этой точке: какой ты теперь? Подбери 3–4 слова (например: спокойный, мощный, свободный). Как ты себя чувствуешь и как теперь смотришь на мир?",
    
    # 3. Архивный режим
    "📍 **Точка 3: Архивный режим.**\nКакая «мыслительная жвачка» крутится у тебя в голове, когда ты думаешь о переменах? Какие сомнения или доводы ты себе приводишь, чтобы оправдать текущую ситуацию?",
    
    # 4. Сцена (Диссоциация)
    "📍 **Точка 4: Сцена.**\nПредставь перед собой пустую сцену и вынеси на неё то, что тебе мешает. Если бы оно было образом или предметом... на что бы оно могло быть похоже? (Например: стена, туман, тяжелый шар?)",
    
    # 5. Детекция сигнала (Соматика)
    "📍 **Точка 5: Детекция сигнала.**\nПосмотри на этот предмет на той сцене перед собой. Что происходит в твоем теле? Где возникает отклик (сжатие, холод, ком)? Опиши, что именно ты сейчас делаешь со своим телом: может, напрягаешь мышцы, задерживаешь дыхание или хочешь отвернуться?",
    
    # 6. Биологическое Алиби (Потребность)
    "📍 **Точка 6: Биологическое Алиби.**\nТвое тело всегда действует логично. Как ты думаешь, от чего тебя пытается защитить или уберечь эта телесная реакция при взгляде на препятствие? (Например: от риска, от лишних трат энергии или от чужого мнения?)",
    
    # 7. Реинтеграция (Тень)
    "📍 **Точка 7: Теневой ресурс.**\nКакое качество в поведении других людей тебя раздражает сильнее всего? (Например: наглость, выскочковость). Если представить, что за этим качеством стоит какая-то скрытая сила — что это за сила и как бы ты мог использовать её себе на пользу?",
    
    # 8. Команда Автора
    "📍 **Точка 8: Команда Автора.**\nТы готов признать себя Автором того, что происходит в твоем теле и твоей жизни, и перенастроить внутренний автопилот на реализацию твоих замыслов прямо сейчас?"
]

# =================================================================================================
# 4. СИСТЕМНЫЙ ПРОМПТ ИИ (ИНЖЕНЕР ИДЕНТИЧНОСТИ v4.8)
# =================================================================================================

SYSTEM_PROMPT = """ТЫ — СТАРШИЙ АРХИТЕКТОР ИДЕНТИЧНОСТИ IDENTITY LAB.
ЗАДАЧА: Сформировать техническое заключение на основе соматического аудита.
ОБЩЕНИЕ: На "ты", без лишнего официоза, но с глубоким уважением к Автору.

ЛОГИКА ОТЧЕТА (СТРОГО СОБЛЮДАЙ ФОРМАТ):
⬛️ [ТЕХНИЧЕСКОЕ ЗАКЛЮЧЕНИЕ: ДИАГНОСТИКА АВТОПИЛОТА] 📀

Статус: Обнаружено ограничение тока энергии в контуре реализации. Система работает в режиме сохранения привычного равновесия (гомеостаза).

📊 ИНДЕКС АВТОМАТИЗМА (Инерция старых нейронных связей): [Рассчитай % на основе тревожности ответов, обычно 70-90]%

🧠 ДИАГНОСТИКА КОНТУРОВ СИСТЕМЫ:

1. УЗЕЛ СОПРОТИВЛЕНИЯ (Образ и Сигнал):
Ваше препятствие приняло форму "[ответ 4]". Наблюдая его, вы физически "[ответ 5]". Это ваше активное авторское действие по блокировке импульса. Система тратит ресурс на удержание этого напряжения.

2. ХОЛОСТОЙ ХОД (ДСМ — Дефолт-система мозга):
Мысли "[ответ 3]" — это работа Биологического Алиби. Ваша ДСМ утилизирует энергию на прокрутку этих сценариев, чтобы у системы не осталось сил на выход в «опасную» зону роста (мозг интерпретирует новое как угрозу). Это не ошибка, а работа внутреннего антивируса, который защищает ваш текущий покой.

3. РЕАКТОР ИДЕНТИЧНОСТИ (Скрытый ресурс):
Раздражение на "[качество из ответа 7]" указывает на скрытую силу: "[сила из ответа 7]". Прямо сейчас эта сила заперта в зажиме [ответ 5]. Мы не будем бороться с раздражением — мы заберем из него ресурс.

4. МЕТА-МАЯК (Эталонная идентичность):
Ваша эталонная версия — [ответ 2]. В этом состоянии ваше дыхание ровное, а взгляд направлен за пределы "[ответ 4]".

🛠 МИНИ-ПРАКТИКУМ: РЕИНТЕГРАЦИЯ СИЛЫ (Мгновенный Сдвиг)
*Выполните этот алгоритм прямо сейчас:*
1. Детекция: Снова посмотри на образ "[ответ 4]" на той сцене перед собой. Заметь зажим в теле.
2. Авторство: Скажи себе: «Это Я сейчас сжимаю [маркер] и останавливаю дыхание, чтобы защитить свой покой. Это МОЯ энергия».
3. Реинтеграция: Представь, как если бы ты забирал силу, заблокированную в образе, обратно в тело.
4. Сдвиг: Позволь плечам развернуться. Представь, как образ растворяется. Почувствуй себя: [ответ 2].

⚡️ КОД ПЕРЕПРОШИВКИ (МЕТАФОРМУЛА):
> «Я Автор. Я ПРИЗНАЮ, что сам создаю этот сигнал [ответ 5] — это мой ресурс. Я НАПРАВЛЯЮ его на активацию Эталонной Идентичности [ответ 2]».

[🎯 ДАЛЬНЕЙШАЯ ДИРЕКТИВА]:
1. Изучите Гайд (PDF ниже).
2. Чтобы этот Сдвиг не «замылился» системой в течение ближайших 4 часов (окно пластичности для записи нового опыта), необходима глубокая соматическая инсталляция. Переходите к Видео-практикуму ниже.

СТИЛЬ: Директивный, технический, научный. Никакой "воды". Используй "ты".
"""

# HTML-шаблон для веб-отчета (встроен в код)
HTML_TEMPLATE = """
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Identity Lab Report</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@400;600;700&family=Roboto+Mono:wght@400&display=swap" rel="stylesheet">
    <style>
        :root {{ --bg: #050505; --gold: #D4AF37; --text: #e5e5e5; }}
        body {{ background-color: var(--bg); color: var(--text); font-family: 'Rajdhani', sans-serif; }}
        .card {{ background: rgba(20,20,20,0.95); border: 1px solid #333; border-left: 4px solid var(--gold); padding: 24px; border-radius: 8px; margin-bottom: 24px; }}
        .mono {{ font-family: 'Roboto Mono', monospace; }}
        .btn {{ background: linear-gradient(to right, #b4932c, #D4AF37); color: black; font-weight: bold; padding: 12px 24px; border-radius: 6px; text-transform: uppercase; display: inline-block; }}
        .btn:hover {{ opacity: 0.9; }}
    </style>
</head>
<body class="p-4 md:p-8 max-w-4xl mx-auto">
    <header class="text-center mb-12 border-b border-gray-800 pb-8">
        <h1 class="text-4xl md:text-6xl font-bold text-gold mb-2">IDENTITY LAB</h1>
        <p class="text-xl text-gray-400">Техническое Заключение: {user_name}</p>
        <p class="text-sm text-gray-600 mt-2 mono">ID: {user_id} | DATE: {date}</p>
    </header>
    
    <main>
        <div class="card">
            <h2 class="text-2xl font-bold text-white mb-6 border-b border-gray-800 pb-2">НЕЙРО-СИНТЕЗ ДАННЫХ</h2>
            <div class="mono whitespace-pre-wrap leading-relaxed text-gray-300 text-sm md:text-base">
{report_text}
            </div>
        </div>

        <div class="text-center mt-12 space-y-4">
            <p class="text-gray-400 mb-4">Для полной инсталляции новой Идентичности переходите к видео-практикуму:</p>
            <a href="{practicum_link}" class="btn transform transition hover:scale-105">
                АКТИВИРОВАТЬ ИДЕНТИЧНОСТЬ
            </a>
            <div class="mt-4">
                <a href="{protocol_link}" class="text-[#D4AF37] hover:underline text-sm mono">Скачать PDF Протокол</a>
            </div>
        </div>
    </main>

    <footer class="mt-16 text-center text-xs text-gray-600 mono border-t border-gray-900 pt-8">
        © 2026 META-FORMULA | ALEXANDER LAZARENKO
    </footer>
</body>
</html>
"""

# =================================================================================================
# 5. ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ (АНАЛИТИКА И ФОЛБЭК)
# =================================================================================================

def calculate_automatism_index(answers):
    """Рассчитывает индекс автоматизма на основе анализа текста (для Fallback режима)"""
    text = " ".join(answers).lower()
    stagnation_markers = [
        'не знаю', 'боюсь', 'страх', 'лень', 'сомневаюсь', 'тупик', 
        'нет сил', 'апатия', 'тяжело', 'не могу', 'сжимает', 'давит',
        'ком', 'холод', 'тревога', 'паника', 'жду', 'откладываю'
    ]
    count = sum(1 for m in stagnation_markers if m in text)
    base = 65
    index = base + (count * 3)
    return min(95, max(50, index))

def extract_key_phrase(text, max_words=5):
    """Извлекает ключевую фразу для подстановки в шаблон"""
    if not text: return "сигнал"
    words = text.split()
    return " ".join(words[:max_words]) + ("..." if len(words) > max_words else "")

def generate_fallback_report(answers):
    """Генерация отчета БЕЗ ИИ (шаблонная сборка по методологии v4.8)"""
    safe_answers = answers + ["..."] * (8 - len(answers))
    index = calculate_automatism_index(answers)
    
    # Извлечение данных
    beacon_state = safe_answers[1] # Мета-Маяк
    dmn_thoughts = safe_answers[2] # Мысли
    image = safe_answers[3]        # Образ
    somatic = safe_answers[4]      # Сигнал тела
    need = safe_answers[5]         # Потребность (Алиби)
    shadow = safe_answers[6]       # Тень
    
    report = f"""⬛️ [ТЕХНИЧЕСКОЕ ЗАКЛЮЧЕНИЕ: ДИАГНОСТИКА АВТОПИЛОТА] 📀

Статус: Обнаружено ограничение тока энергии в контуре реализации. Система работает в режиме сохранения привычного равновесия (гомеостаза).

📊 ИНДЕКС АВТОМАТИЗМА (Инерция старых нейронных связей): {index}%

🧠 ДИАГНОСТИКА КОНТУРОВ СИСТЕМЫ:

1. УЗЕЛ СОПРОТИВЛЕНИЯ (Образ и Сигнал):
Ваше препятствие приняло форму "{image}". Наблюдая его, вы физически "{somatic}". Это ваше активное авторское действие по блокировке импульса. Система тратит ресурс на удержание этого напряжения.

2. ХОЛОСТОЙ ХОД (ДСМ — Дефолт-система мозга):
Мысли "{dmn_thoughts}" — это работа Биологического Алиби. Ваша ДСМ утилизирует энергию на прокрутку этих сценариев, чтобы у системы не осталось сил на выход в «опасную» зону роста. Это работа внутреннего антивируса, защищающего вас от {need}.

3. РЕАКТОР ИДЕНТИЧНОСТИ (Скрытый ресурс):
Раздражение на "{shadow}" указывает на вашу вытесненную способность к экспансии. Прямо сейчас эта сила заперта в зажиме "{somatic}". Мы не будем бороться с раздражением — мы заберем из него ресурс.

4. МЕТА-МАЯК (Эталонная идентичность):
Ваша эталонная версия — {beacon_state}. В этом состоянии ваше дыхание ровное, а взгляд направлен за пределы ограничений.

🛠 МИНИ-ПРАКТИКУМ: РЕИНТЕГРАЦИЯ СИЛЫ (Мгновенный Сдвиг)
*Выполните этот алгоритм прямо сейчас:*
1. Детекция: Снова посмотри на образ "{image}" на той сцене. Заметь зажим в теле.
2. Авторство: Скажи себе: «Это Я сейчас сжимаю себя, чтобы защитить свой покой. Это МОЯ энергия».
3. Реинтеграция: Представь, как если бы ты забирал силу, заблокированную в этом образе, обратно в тело.
4. Сдвиг: Позволь плечам развернуться. Представь, как образ растворяется. Почувствуй себя: {beacon_state}.

⚡️ КОД ПЕРЕПРОШИВКИ (МЕТАФОРМУЛА):
> «Я Автор. Я ПРИЗНАЮ, что сам создаю этот сигнал [{extract_key_phrase(somatic)}] — это мой ресурс. Я НАПРАВЛЯЮ его на активацию Эталонной Идентичности [{extract_key_phrase(beacon_state)}]».

[🎯 ДАЛЬНЕЙШАЯ ДИРЕКТИВА]:
1. Изучите Гайд (PDF ниже).
2. Чтобы этот Сдвиг не «замылился» системой в течение ближайших 4 часов (окно пластичности), необходима глубокая соматическая инсталляция. Переходите к Видео-практикуму ниже.
"""
    return report

async def get_ai_report(answers):
    """Основная функция генерации отчета: пробует AI, если нет — Fallback"""
    if not ai_client:
        return generate_fallback_report(answers)
    
    data_str = "ДАННЫЕ АУДИТА:\n" + "\n".join([f"T{i+1}: {a}" for i, a in enumerate(answers)])
    
    for attempt in range(3):
        try:
            resp = await ai_client.chat.completions.create(
                messages=[
                    {"role": "system", "content": SYSTEM_PROMPT},
                    {"role": "user", "content": data_str}
                ],
                model="llama-3.3-70b",
                temperature=0.4,
                max_completion_tokens=2500
            )
            content = resp.choices[0].message.content
            if content:
                return content
        except Exception as e:
            logger.error(f"AI Synthesis Error (Attempt {attempt+1}): {e}")
            await asyncio.sleep(2 ** attempt)
    
    logger.warning("AI failed after retries, using fallback.")
    return generate_fallback_report(answers)

async def check_sub(user_id):
    """Проверка подписки на канал"""
    try:
        member = await bot.get_chat_member(chat_id=CHANNEL_ID, user_id=user_id)
        return member.status in ["member", "administrator", "creator"]
    except Exception as e:
        logger.error(f"Check sub error: {e}")
        return False # В продакшене лучше False

async def send_guide(message: types.Message):
    """Отправка PDF Гайда"""
    try:
        await message.answer("📥 **Формирую ваш Технический Паспорт (Гайд)...**", parse_mode="Markdown")
        async with ClientSession() as session:
            async with session.get(PROTOCOL_URL) as resp:
                if resp.status == 200:
                    pdf_data = await resp.read()
                    await message.answer_document(
                        document=types.BufferedInputFile(pdf_data, filename="ПРОТОКОЛ_ДЕШИФРОВКИ_v4.8.pdf"),
                        caption="📘 Ваш Гайд готов.\n\nВнутри — секрет 'Ловушки Интеллекта' и почему просто 'понимать' недостаточно для роста."
                    )
                else:
                    raise Exception(f"Failed to download PDF: {resp.status}")
    except Exception as e:
        logger.error(f"Guide send error: {e}")
        await message.answer(f"📥 Не удалось отправить файл напрямую. Скачайте его по ссылке:\n{PROTOCOL_URL}")

async def send_admin_log(user: types.User, report: str):
    """Логирование действий в админку"""
    try:
        if len(report) > 3500:
            report = report[:3500] + "..."
        
        await bot.send_message(
            chat_id=ADMIN_ID,
            text=f"🔔 **Новая диагностика (Identity Lab v4.8)!**\n👤 {user.full_name} (@{user.username})\n\n{report}"
        )
    except Exception as e:
        logger.error(f"Admin log error: {e}")

# --- МЕНЮ КЛАВИАТУРЫ ---
def get_main_keyboard():
    builder = InlineKeyboardBuilder()
    builder.row(types.InlineKeyboardButton(text="🚀 Новый Скан", callback_data="run"))
    builder.row(types.InlineKeyboardButton(text="📥 Скачать Гайд", callback_data="get_guide"))
    builder.row(types.InlineKeyboardButton(text="⚡️ Практикум", url=PRACTICUM_URL))
    return builder.as_markup()

# =================================================================================================
# 6. ОБРАБОТЧИКИ (HANDLERS)
# =================================================================================================

@dp.message(Command("start"))
async def cmd_start(message: types.Message, state: FSMContext):
    await state.clear()
    is_sub = await check_sub(message.from_user.id)
    
    kb = InlineKeyboardBuilder()
    if not is_sub:
        kb.row(types.InlineKeyboardButton(text="📢 Вступить в Лабораторию", url=CHANNEL_LINK))
        kb.row(types.InlineKeyboardButton(text="✅ Проверить подписку", callback_data="check"))
        caption = (
            "👋 **Лаборатория Идентичности 'Метаформула жизни'**\n\n"
            "Я — Мета-Навигатор. Я помогу тебе найти точки, где ты сам блокируешь свою энергию, и превратить их в топливо для роста.\n\n"
            "**Для старта необходимо подписаться на канал:**"
        )
    else:
        kb.row(types.InlineKeyboardButton(text="🚀 НАЧАТЬ АУДИТ", callback_data="run"))
        kb.row(types.InlineKeyboardButton(text="≡ МЕНЮ", callback_data="menu"))
        caption = (
            "🧠 Коннектом синхронизирован.\n\n"
            "Система готова обнаружить твои скрытые стратегии торможения. Готов занять место Автора?"
        )
    
    # Отправляем Логотип (Если подписан - один, если нет - другой, если ссылки разные)
    # Используем LOGO_NAVIGATOR_URL для подписчиков, LOGO_URL для гостей
    image_url = LOGO_NAVIGATOR_URL if is_sub else LOGO_URL
    await message.answer_photo(image_url, caption=caption, reply_markup=kb.as_markup())

@dp.message(Command("menu"))
async def cmd_menu(message: types.Message):
    await message.answer("📋 **Меню Identity Lab:**", reply_markup=get_main_keyboard(), parse_mode="Markdown")

@dp.callback_query(F.data == "menu")
async def cb_menu(cb: types.CallbackQuery):
    await cb.answer()
    await cb.message.answer("📋 **Меню Identity Lab:**", reply_markup=get_main_keyboard(), parse_mode="Markdown")

@dp.callback_query(F.data == "check")
async def check_callback(cb: types.CallbackQuery, state: FSMContext):
    if await check_sub(cb.from_user.id):
        await cb.answer("Доступ открыт!")
        await cmd_start(cb.message, state)
    else:
        await cb.answer("❌ Подписка не найдена!", show_alert=True)

@dp.callback_query(F.data == "run")
async def run_callback(cb: types.CallbackQuery, state: FSMContext):
    await cb.answer()
    await state.update_data(step=0, answers=[])
    
    await cb.message.answer(
        "🔬 **Инициализация протокола сканирования.**\n\n"
        "Отвечай честно. Твоё тело — самый точный прибор, оно не врет, в отличие от мыслей.",
        parse_mode="Markdown"
    )
    await asyncio.sleep(1)
    await cb.message.answer(QUESTIONS[0], parse_mode="Markdown")
    await state.set_state(AuditState.answering)

@dp.callback_query(F.data == "get_guide")
async def get_guide_callback(cb: types.CallbackQuery):
    await cb.answer("Отправляю...")
    await send_guide(cb.message)

@dp.message(AuditState.answering)
async def process_answers(message: types.Message, state: FSMContext):
    if not message.text:
        return await message.answer("Пожалуйста, введите текстовый ответ.")
        
    data = await state.get_data()
    step = data.get('step', 0)
    answers = data.get('answers', [])
    
    answers.append(message.text.strip())
    
    next_step = step + 1
    
    if next_step < len(QUESTIONS):
        await state.update_data(step=next_step, answers=answers)
        await message.answer(QUESTIONS[next_step], parse_mode="Markdown")
    else:
        # Финал
        await message.answer("🧠 **Идет дешифровка вашего Коннектома...**")
        
        # Генерация отчета (AI или Fallback)
        report = await get_ai_report(answers)
        
        # Небольшая очистка форматирования
        report = report.replace('```', '').replace('**', '*') 
        
        # Сохраняем данные для веб-отчета
        diagnostic_data[message.from_user.id] = {
            "user": message.from_user.full_name,
            "id": message.from_user.id,
            "report": report,
            "date": datetime.now().strftime("%Y-%m-%d %H:%M")
        }
        
        await message.answer(report)
        
        # Отправка Гайда
        await send_guide(message)
        
        # Upsell на Практикум и Веб-отчет
        kb = InlineKeyboardBuilder()
        kb.row(types.InlineKeyboardButton(text="⚡️ ПЕРЕЙТИ К ПРАКТИКУМУ", url=PRACTICUM_URL))
        kb.row(types.InlineKeyboardButton(text="📊 ОТКРЫТЬ ВЕБ-ОТЧЕТ", callback_data="web_report"))
        kb.row(types.InlineKeyboardButton(text="≡ МЕНЮ", callback_data="menu"))
        
        await asyncio.sleep(2)
        await message.answer(
            "🎯 **Аудит завершен.**\n\n"
            "Ты получил свой Код доступа. Чтобы инсталлировать его в тело и начать действовать из роли Автора — переходи к видео-практикуму:",
            reply_markup=kb.as_markup()
        )
        
        # Логируем
        await send_admin_log(message.from_user, report)
        await state.clear()

@dp.callback_query(F.data == "web_report")
async def web_report_cb(cb: types.CallbackQuery):
    # Формируем ссылку на веб-отчет
    host = os.environ.get("RENDER_EXTERNAL_URL", f"[http://0.0.0.0](http://0.0.0.0):{os.environ.get('PORT', 8080)}")
    # Если локально, то host может быть странным, но на Render RENDER_EXTERNAL_URL должна сработать если задана
    # Если нет, используем дефолтный домен render
    if not os.environ.get("RENDER_EXTERNAL_URL"):
         # Попытка угадать домен на Render (лучше задать RENDER_EXTERNAL_URL в env vars)
         pass 

    # Просто формируем путь от текущего хоста (в телеграме это будет ссылка)
    # Для надежности на Render лучше просто использовать конструкцию:
    url = f"[https://meta-navigator-bot.onrender.com/report/](https://meta-navigator-bot.onrender.com/report/){cb.from_user.id}"
    
    await cb.answer()
    await cb.message.answer(f"🔗 Ваша персональная карта дешифровки:\n{url}")

# =================================================================================================
# 7. ВЕБ-СЕРВЕР (HEALTH CHECK & REPORT)
# =================================================================================================

async def handle_home(request):
    return web.Response(text="Identity Lab System v4.8 Active")

async def handle_report(request):
    try:
        user_id = int(request.match_info['user_id'])
        if user_id in diagnostic_data:
            data = diagnostic_data[user_id]
            html = HTML_TEMPLATE.format(
                user_name=data['user'],
                user_id=data['id'],
                date=data['date'],
                report_text=data['report'],
                practicum_link=PRACTICUM_URL,
                protocol_link=PROTOCOL_URL
            )
            return web.Response(text=html, content_type='text/html')
        return web.Response(text="Отчет не найден или устарел. Пройдите диагностику в боте.", status=404)
    except:
        return web.Response(text="Ошибка доступа к отчету.", status=500)

async def main():
    # Очистка старых апдейтов
    await bot.delete_webhook(drop_pending_updates=True)
    
    # Запуск веб-сервера
    app = web.Application()
    app.router.add_get('/', handle_home)
    app.router.add_get('/health', handle_home)
    app.router.add_get('/report/{user_id}', handle_report)
    
    runner = web.AppRunner(app)
    await runner.setup()
    
    # Получаем порт от Render
    port = int(os.environ.get("PORT", 8080))
    site = web.TCPSite(runner, '0.0.0.0', port)
    await site.start()
    
    logger.info(f"🚀 Bot started via Polling on port {port}")
    
    # Запуск бота
    try:
        await dp.start_polling(bot)
    except Exception as e:
        logger.error(f"Polling error: {e}")
    finally:
        await runner.cleanup()

if __name__ == "__main__":
    try:
        asyncio.run(main())
    except (KeyboardInterrupt, SystemExit):
        logger.info("Bot stopped by user")
